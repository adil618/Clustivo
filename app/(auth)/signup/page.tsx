import { SignupForm } from "@/components/signup-form";

export const metadata = {
  title: "Create Account – Clustivo",
  description: "Sign up for Clustivo and find people who share your interests.",
};

export default function SignupPage() {
  return <SignupForm />;
}
